import React, { FC } from 'react';
import './Logo.css';

const Logo: FC = () => {
    return (
        <div className="logo-container">
            <span className="logo-text">Interviewer</span>
        </div>
    );
};

export default Logo;
